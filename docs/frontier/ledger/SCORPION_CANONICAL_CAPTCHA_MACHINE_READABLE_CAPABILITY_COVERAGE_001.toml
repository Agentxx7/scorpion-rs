id = "SCORPION_CANONICAL_CAPTCHA_MACHINE_READABLE_CAPABILITY_COVERAGE_001"
sdd = "docs/frontier/CANONICAL_CAPTCHA_PRODUCTION_CAPABILITY_SDD.md"
summary = "Machine-readable, harness-checked description of the already-shipped CAPTCHA production chain: public provider selection through Chrome execution, challenge detection (top-level / same-session frame / OOPIF), snapshot materialization, provider routing, real PaliGemma local inference, canonical solution, browser action, and post-action observation."
# This is the harness-computed TRUE stage, not an aspirational claim.
# PRODUCTION_REACHABLE.verdict below is MET, but the real production
# behavior past DetectedBrowserChallenge::route (provider registry
# construction, including PaliGemma) routes through a method call on a
# local variable this harness's AST adjacency check cannot bind to a
# type — see [stages.WIRED]'s own comment and this entry's SDD section 3.
# CLOSED (SCORPION_CANONICAL_CAPTCHA_CI_ENFORCED_AND_CLOSED_
# ACTIVATION_001): the CAPTCHA CI-portable evidence step (spider_core
# job, .github/workflows/rust.yml) has genuinely executed successfully
# on real remote GitHub Actions CI, twice independently — run
# 32927984609 (commit 97092abc, 25 passed / 0 failed / 2 ignored,
# cited by [proof.CI_PROVEN]) and again on the exact commit that added
# [stages.CI_ENFORCED] itself (73ee4ac3, cited by [stages.CLOSED].
# closed_commit), confirming the lifecycle-metadata change did not
# itself regress anything — after two real, independent, previously-
# unexercised CI defects were found and fixed on the way there (a
# shallow-clone commit-reachability gap blocking the whole closure/
# architecture assurance suite, and a missing pre-build + missing
# thread-stack-size accommodation specific to this step; see
# SCORPION_CANONICAL_CI_DETERMINISTIC_NETWORK_LOCKDOWN_EXECUTION_001's
# own closure report for the full trace), and one real, generic
# verifier grammar defect found and fixed (executable_test_command/
# parse_test_selection could not recognize the namespace-isolation
# prefix when an additional literal env-assignment token like
# RUST_MIN_STACK=<value> followed it; see commit 4c0bfbf5). See
# [stages.CI_ENFORCED], [proof.CI_PROVEN], and [stages.CLOSED] below
# for the machine-readable binding.
stage = "CLOSED"
required_proof_classes = ["CODE_PROVEN", "OPERATOR_OBSERVED", "LIVE_ENVIRONMENT_DEPENDENT"]
# CI_PROVEN is deliberately NOT added here: `required_proof_classes` is
# a top-level field, not one of the LEDGER_ATTESTATION_ONLY_STAGES/
# LEDGER_ATTESTATION_ONLY_PROOF_CLASSES fields `ledger_toml_unchanged_
# except_attestation_fields` strips before comparing a cited
# reviewed_commit's own ledger content against current HEAD — changing
# it would make reviewed_commit's self-reference constraint
# unsatisfiable by any real, already-existing commit (no prior commit
# could ever have "CI_PROVEN" already listed, and a commit cannot cite
# its own not-yet-computed hash). [proof.CI_PROVEN] below is real,
# genuine, machine-readable evidence regardless of whether it is
# "required" — required_proof_classes only governs closed_requires_
# every_declared_proof_class_independently's CLOSED-time completeness
# check, not whether a present, well-formed proof record is honored.

[proof.CODE_PROVEN]
capability_id = "SCORPION_CANONICAL_CAPTCHA_MACHINE_READABLE_CAPABILITY_COVERAGE_001"
commit = "d85cf3d02ed28656d76dfa9bcb69e015c25cf83c"
evidence = [
    "every IMPLEMENTED symbol below resolves to a real, non-comment, non-test Rust definition, AST-verified",
    "the WIRED chain from Website::crawl to detect_browser_challenge is AST-proven, non-macro, hop by hop",
    "spider_cli and spider_mcp both call Website::crawl with chrome compiled in by default (Cargo.toml-verified feature forwarding)",
]

[[proof.OPERATOR_OBSERVED]]
capability_id = "SCORPION_CANONICAL_CAPTCHA_MACHINE_READABLE_CAPABILITY_COVERAGE_001"
commit = "d85cf3d02ed28656d76dfa9bcb69e015c25cf83c"
command = "scorpion --headless --captcha-provider paligemma-local --url http://127.0.0.1:<port>/ --limit 1 crawl"
purpose = "Real shipping CLI proof: genuine OOPIF challenge, real Chrome, real CUDA/F16 PaliGemma inference, no injected solution."
result = "captcha-observation: Framed { materialized: true, route_outcome: SolutionProduced { action: Applied { actions_applied: 1, challenge_observed_after_action: false } } } — observed twice, exit 0 both times, during SCORPION_CANONICAL_CHROME_OOPIF_TARGET_VISIBILITY_IN_STREAMING_PIPELINE_001."

[[proof.OPERATOR_OBSERVED]]
capability_id = "SCORPION_CANONICAL_CAPTCHA_MACHINE_READABLE_CAPABILITY_COVERAGE_001"
commit = "d85cf3d02ed28656d76dfa9bcb69e015c25cf83c"
command = "spider-mcp (stdio JSON-RPC): initialize -> tools/call spider_crawl {url, limit:1, headless:true, captcha_provider:\"paligemma-local\"} -> tools/list"
purpose = "Real shipping MCP proof: same genuine OOPIF challenge and real CUDA/F16 PaliGemma inference, through the actual spider-mcp binary, then confirming the server remains responsive."
result = "captcha_observation field: \"Framed { ... materialized: true, route_outcome: SolutionProduced { action: Applied { actions_applied: 1, challenge_observed_after_action: false } } }\"; tools/list afterward returned 12 tools."

[[proof.OPERATOR_OBSERVED]]
capability_id = "SCORPION_CANONICAL_CAPTCHA_MACHINE_READABLE_CAPABILITY_COVERAGE_001"
commit = "d85cf3d02ed28656d76dfa9bcb69e015c25cf83c"
command = "cargo test -p spider --test captcha_browser_oopif_streaming_shipping_real --test captcha_browser_paligemma_real --test captcha_browser_production_binding_real --features \"chrome local_paligemma local_paligemma_cuda\" -- --exact <test-name> --nocapture, with SCORPION_PALIGEMMA_PINNED_ARTIFACTS set to a real pinned checkpoint and a real CUDA-capable GPU present"
purpose = "The THREE genuinely hardware-dependent, #[ignore]d tests excluded from CI-portable VERIFIED evidence (SCORPION_CANONICAL_CAPTCHA_CI_ENFORCEABLE_EVIDENCE_PARTITION_001; an earlier version of this record named only two and omitted the third, corrected here per SCORPION_CANONICAL_CAPTCHA_CI_ENFORCED_AND_CLOSED_ACTIVATION_001's own independent audit) — captcha_browser_oopif_streaming_shipping_real.rs::website_crawl_shipping_pipeline_detects_and_solves_a_genuine_oopif_challenge, captcha_browser_paligemma_real.rs::real_browser_snapshot_paligemma_inference_and_exact_action, and captcha_browser_production_binding_real.rs::real_paligemma_solution_reaches_the_browser_and_clicks_the_target (pre-existing #[ignore], predates this ledger entry, never previously named here at all) — encode the same real production capability the two CLI/MCP OPERATOR_OBSERVED records above already establish end to end; named here explicitly so this ledger entry never loses track of any of them merely because they are absent from VERIFIED."
result = "real_browser_snapshot_paligemma_inference_and_exact_action: previously observed passing against real CUDA/F16 hardware (carried forward from this capability's own prior frontier) — the only one of the three with its own separately-claimed observed pass. website_crawl_shipping_pipeline_detects_and_solves_a_genuine_oopif_challenge and real_paligemma_solution_reaches_the_browser_and_clicks_the_target: both exercise the identical detect -> route -> PaliGemma -> action chain the CLI/MCP records above already proved genuinely solves a real challenge; neither test's own direct pass has been separately observed/confirmed within this session's own evidence trail (for the OOPIF test, see SCORPION_CANONICAL_CHROME_OOPIF_TARGET_VISIBILITY_IN_STREAMING_PIPELINE_001's own regression notes on host RAM conditions). The underlying capability is OPERATOR_OBSERVED via the CLI/MCP records; these two specific tests' own passes are correctly classified as real-hardware-dependent (existence + classification only), never fabricated as separately observed."

[proof.LIVE_ENVIRONMENT_DEPENDENT]
capability_id = "SCORPION_CANONICAL_CAPTCHA_MACHINE_READABLE_CAPABILITY_COVERAGE_001"
requirements = ["real headless Chrome", "real CUDA-capable GPU", "pinned local PaliGemma checkpoint (SCORPION_PALIGEMMA_PINNED_ARTIFACTS)"]
observed = false # classification is not observation; see proof.OPERATOR_OBSERVED for the actual observations

[proof.CI_PROVEN]
capability_id = "SCORPION_CANONICAL_CAPTCHA_MACHINE_READABLE_CAPABILITY_COVERAGE_001"
commit = "97092abcfa536828e21eb1a5ba54a8875e33f55b"
run_commit = "97092abcfa536828e21eb1a5ba54a8875e33f55b"
workflow = ".github/workflows/rust.yml"
run_id = "32927984609"
run_url = "https://github.com/Agentxx7/scorpion-rs/actions/runs/32927984609"
job = "spider_core"
step = "Cargo test CAPTCHA CI-portable evidence (chrome local_paligemma)"
command_identity = "package=spider;lib=false;tests=browser_challenge_detection_real,captcha_browser_binding_real,captcha_browser_frame_action_real,captcha_browser_oopif_action_real,captcha_browser_oopif_streaming_shipping_real,captcha_browser_production_binding_real;features=chrome,local_paligemma;filters=;exact=false;skip="
conclusion = "success"
result = "25 passed, 0 failed, 2 ignored (the same two genuinely hardware-dependent tests VERIFIED/OPERATOR_OBSERVED already classify)."
note = "This exact commit (97092abc) predates the harness grammar fix (4c0bfbf5, SCORPION_CANONICAL_CAPTCHA_CI_ENFORCED_AND_CLOSED_ACTIVATION_001) that lets closure_harness.rs itself structurally recognize this step's real run: text — the run's own success is unaffected by that fix, which only concerns how this ledger's evidence is later verified, not what genuinely executed on GitHub's infrastructure at the time."

[stages.DESIGNED]
sdd = "docs/frontier/CANONICAL_CAPTCHA_PRODUCTION_CAPABILITY_SDD.md"

[stages.IMPLEMENTED]
# `resolve_paligemma_provider`/`paligemma_provider`/`PaligemmaLocalCaptchaProvider`/
# its `solve` method are all `#[cfg(all(feature = "chrome", feature =
# "local_paligemma"))]`-gated. `local_paligemma` deliberately stays out of
# [stages.PRODUCTION_REACHABLE].feature_requirements below (that list
# feeds the reachability verdict's ANY-of-these-enabled check, and
# `local_paligemma` is opt-in, not part of either shipping artifact's
# default build) — declared here instead, exactly the escape hatch
# `declared_features_for_implemented` documents.
additional_cfg_features = ["local_paligemma"]
evidence = [
    "spider/src/configuration.rs:chrome_fetch_params",
    "spider/src/features/browser_challenge_detection.rs:detect_browser_challenge",
    "spider/src/features/browser_challenge_detection.rs:probe_oopif_challenges",
    "spider/src/features/browser_challenge_detection.rs:has_potential_oopif_candidate",
    "spider/src/features/browser_challenge_detection.rs:materialize_framed_challenge",
    "spider/src/features/browser_challenge_detection.rs:route",
    "spider/src/features/browser_challenge.rs:capture",
    "spider/src/features/browser_challenge.rs:capture_in_frame",
    "spider/src/features/solvers.rs:route_detected_browser_challenge",
    "spider/src/features/solvers.rs:route_detected_framed_browser_challenge",
    "spider/src/features/solvers.rs:resolve_paligemma_provider",
    "spider/src/features/solvers.rs:paligemma_provider",
    "spider/src/features/captcha_browser.rs:execute_browser_captcha_attempt",
    "spider/src/features/captcha_browser.rs:execute_browser_captcha_attempt_in_frame",
    "spider/src/features/frame_context.rs:resolve_top_level",
    "spider/src/features/frame_context.rs:resolve_child",
    "spider/src/features/frame_context.rs:resolve_same_session_child",
    "spider/src/features/paligemma_captcha.rs:PaligemmaLocalCaptchaProvider",
    "spider/src/features/paligemma_captcha.rs:solve",
]

[stages.VERIFIED]
# Deliberately CI-portable only (SCORPION_CANONICAL_CAPTCHA_CI_ENFORCEABLE_
# EVIDENCE_PARTITION_001): every item below needs real headless Chrome
# (present on ubuntu-latest GitHub-hosted runners) and `local_paligemma`
# compiled in (several files import `PaligemmaLocalCaptchaProvider` even
# where a given test injects a deterministic `FixedPointProvider`
# instead), but never `local_paligemma_cuda`, never a GPU, never
# `SCORPION_PALIGEMMA_PINNED_ARTIFACTS`, never the 13.5GB RAM preflight —
# confirmed by actually running this exact evidence set with that env var
# unset and only `--features "chrome local_paligemma"` (no `_cuda`)
# enabled. The THREE genuinely hardware-dependent, `#[ignore]`d tests
# this file family also contains — corrected count, an earlier version
# of this comment said "two" and omitted the third entirely —
# (`captcha_browser_oopif_streaming_shipping_real.rs::website_crawl_
# shipping_pipeline_detects_and_solves_a_genuine_oopif_challenge`,
# `captcha_browser_production_binding_real.rs::real_paligemma_solution_
# reaches_the_browser_and_clicks_the_target` — pre-existing, `#[ignore]`d
# before this frontier and never cited here either, and
# `captcha_browser_paligemma_real.rs::real_browser_snapshot_paligemma_
# inference_and_exact_action`) are deliberately NOT evidence here. They
# are never deleted or forgotten, never CI-executed evidence — carried
# under `proof.OPERATOR_OBSERVED`/`proof.LIVE_ENVIRONMENT_DEPENDENT`
# below instead, which distinguishes real classification from an
# actually-observed pass per test (only one of the three has a carried-
# forward observed pass; the other two are classified, not separately
# claimed as observed — see that record's own text).
test_only = true
evidence = [
    "spider/tests/browser_challenge_detection_real.rs:case_a_normal_page_is_not_detected",
    "spider/tests/browser_challenge_detection_real.rs:case_b_supported_challenge_is_detected_and_materialized",
    "spider/tests/browser_challenge_detection_real.rs:case_c_text_only_false_positive_is_not_detected",
    "spider/tests/browser_challenge_detection_real.rs:case_d_framed_challenge_is_detected_with_correct_frame_identity",
    "spider/tests/browser_challenge_detection_real.rs:case_e_detector_failure_is_typed_not_no_challenge",
    "spider/tests/browser_challenge_detection_real.rs:case_f_detection_performs_no_browser_mutation",
    "spider/tests/captcha_browser_binding_real.rs:controlled_browser_binding_applies_all_forms_and_fails_closed",
    "spider/tests/captcha_browser_frame_action_real.rs:frame_action_integration_proof_correct_point_solves_the_iframe",
    "spider/tests/captcha_browser_frame_action_real.rs:raw_top_level_click_at_frame_local_coordinates_does_not_solve_the_iframe",
    "spider/tests/captcha_browser_frame_action_real.rs:detection_materializes_the_same_session_child_with_correct_identity",
    "spider/tests/captcha_browser_frame_action_real.rs:stale_frame_id_fails_typed_with_no_action",
    "spider/tests/captcha_browser_frame_action_real.rs:detached_frame_before_action_fails_typed_with_no_action",
    "spider/tests/captcha_browser_production_binding_real.rs:deterministic_wrong_point_action_negative_test",
    "spider/tests/captcha_browser_production_binding_real.rs:out_of_bounds_solution_dispatches_zero_actions",
    "spider/tests/captcha_browser_production_binding_real.rs:provider_failure_dispatches_zero_actions",
    "spider/tests/captcha_browser_production_binding_real.rs:normal_page_yields_no_detection_and_therefore_no_route_call",
    "spider/tests/captcha_browser_production_binding_real.rs:applied_outcome_carries_a_real_post_action_observation_field",
    "spider/tests/captcha_browser_oopif_action_real.rs:oopif_action_integration_proof_correct_point_solves_the_child",
    "spider/tests/captcha_browser_oopif_action_real.rs:same_point_on_parent_context_does_not_solve_the_oopif",
    "spider/tests/captcha_browser_oopif_action_real.rs:detection_materializes_the_oopif_with_correct_identity",
    "spider/tests/captcha_browser_oopif_action_real.rs:detached_oopif_target_before_action_fails_typed_with_no_action",
    "spider/tests/captcha_browser_oopif_action_real.rs:unowned_target_info_fails_typed_never_a_fallback",
    "spider/tests/captcha_browser_oopif_action_real.rs:multiple_oopifs_action_targets_only_the_correct_one",
    "spider/tests/captcha_browser_oopif_streaming_shipping_real.rs:website_crawl_shipping_pipeline_isolates_the_correct_oopif_among_several",
    "spider/tests/captcha_browser_oopif_streaming_shipping_real.rs:concurrent_website_crawl_requests_never_cross_contaminate_oopif_targets",
]
last_verified_command = "cargo test -p spider --test browser_challenge_detection_real --test captcha_browser_binding_real --test captcha_browser_frame_action_real --test captcha_browser_production_binding_real --test captcha_browser_oopif_action_real --test captcha_browser_oopif_streaming_shipping_real --features \"chrome local_paligemma\""
last_verified_result = "25/25 passed, 2 ignored (real-hardware, out of scope for this CI-portable evidence set), 0 failed — run with SCORPION_PALIGEMMA_PINNED_ARTIFACTS unset and local_paligemma_cuda disabled, confirming no GPU/artifact dependency: 6/6 browser_challenge_detection_real; 1/1 captcha_browser_binding_real; 5/5 captcha_browser_frame_action_real; 5/5 (+1 ignored) captcha_browser_production_binding_real; 6/6 captcha_browser_oopif_action_real; 2/2 (+1 ignored) captcha_browser_oopif_streaming_shipping_real"

[stages.WIRED]
# `Page::new_streaming`/`Page::new_base` are called via qualified
# associated-call syntax (`Page::new_streaming(...)`, `Self::new_base(...)`)
# at their real call sites, not `.method()` syntax, so this chain never
# depends on the receiver-variable-name heuristic `ast_function_calls`
# applies to true method calls. `fetch_page_html`'s real definition is one
# of two mutually exclusive `#[cfg(all(feature = "fs"|"not(fs)", feature =
# "chrome"))]` overloads; `fs` is declared in additional_cfg_features so
# the harness resolves the `fs`-active overload (the one genuinely present
# in this workspace's own default feature set, which includes `tokio/fs`
# transitively) rather than failing on cfg ambiguity.
additional_cfg_features = ["fs"]
callers = [
    "spider/src/website.rs:Website::crawl -> spider/src/website.rs:crawl_concurrent -> spider/src/website.rs:crawl_establish -> spider/src/page.rs:Page::new_streaming -> spider/src/page.rs:Page::new_base -> spider/src/utils/mod.rs:fetch_page_html -> spider/src/utils/mod.rs:fetch_page_html_chrome_base -> spider/src/utils/mod.rs:fetch_page_html_chrome_base_inner -> spider/src/features/browser_challenge_detection.rs:detect_browser_challenge",
]

[stages.PRODUCTION_REACHABLE]
reachability_kind = "binary_default"
shipping_artifacts = ["spider_cli", "spider_mcp"]
feature_requirements = ["chrome"]
entry_point_symbols = ["Website::crawl"]
siblings_enumerated = true
siblings = [
    "spider_worker (default features = [] — chrome not enabled by default; its request handlers also use Page::new_page_streaming directly rather than Website::crawl, so it is not a sibling reachability path for this capability even when chrome is enabled)",
]
siblings_note = "spider_cli and spider_mcp are the only known shipping artifacts whose default feature set both enables chrome and whose own source calls Website::crawl directly. spider_worker does not qualify under either its default (no chrome) or an optional chrome build (different call path, Page::new_page_streaming, not Website::crawl)."
verdict = "MET"
verdict_evidence = "spider_cli/Cargo.toml default = [..., \"chrome\", ...] (line 62) and spider_cli/src/main.rs:crawl_with_mode calls website.crawl().await under #[cfg(feature = \"chrome\")] when headless is requested — the shipping binary's own default build. spider_mcp/Cargo.toml default = [\"chrome\", ...] (line 54) and spider_mcp/src/tools/mod.rs:spawn_crawl_task calls website.crawl().await under the same #[cfg(feature = \"chrome\")] guard when use_headless is true, via a real `use spider::website::Website;` import (spider_mcp/src/tools/mod.rs:19) binding to the canonical external-artifact spelling."

[stages.ADVERSARIALLY_VERIFIED]
capability_id = "SCORPION_CANONICAL_CAPTCHA_MACHINE_READABLE_CAPABILITY_COVERAGE_001"
# Re-attested against f20fb917 (SCORPION_CANONICAL_MCP_BEHAVIORAL_CI_
# ENFORCEMENT_001's failure-diagnostics wiring for the two new
# spider_mcp behavioral-contract steps), which touched .github/
# workflows/rust.yml — this capability's own CI_ENFORCED.ci_workflow_
# file, an unconditional closure_relevant_file regardless of stage —
# invalidating the prior reviewed_commit's (d5765a34) byte-identity
# binding on it, exactly as SCORPION_CANONICAL_CLOSURE_HARNESS_
# REVIEWED_COMMIT_SELF_REFERENCE_001's own drift check requires. Pure
# attestation-only field update: no ADVERSARIALLY_VERIFIED evidence or
# bypass-attempt entry below changed, and no behavior this capability
# depends on changed at all. Confirmed directly: f20fb917's only change
# is redirecting the same two `build`-job steps' output to a log file
# and invoking the pre-existing .github/scripts/report-test-step-
# failure.sh on failure (the same script the `spider_core` job's own
# CAPTCHA CI step already uses) — the `spider_core` job's own CAPTCHA
# CI step (test_targets/feature_set/positional_filters/exact/skip all
# bound by [stages.CI_ENFORCED] below) is byte-for-byte untouched in
# that diff, and no other file changed.
reviewed_commit = "f20fb917825e8ed36c237f4598386052cde5aca7"
bypass_attempts = [
    "inject a deterministic wrong/out-of-bounds click point instead of the real solution -> action rejected or dispatches zero actions, never a false solve (spider/tests/captcha_browser_production_binding_real.rs:deterministic_wrong_point_action_negative_test, out_of_bounds_solution_dispatches_zero_actions; spider/tests/captcha_browser_frame_action_real.rs:raw_top_level_click_at_frame_local_coordinates_does_not_solve_the_iframe; spider/tests/captcha_browser_oopif_action_real.rs:same_point_on_parent_context_does_not_solve_the_oopif)",
    "act through a stale/detached frame or OOPIF target identity -> typed failure, zero actions dispatched, never a fallback to a different target (spider/tests/captcha_browser_frame_action_real.rs:stale_frame_id_fails_typed_with_no_action, detached_frame_before_action_fails_typed_with_no_action; spider/tests/captcha_browser_oopif_action_real.rs:detached_oopif_target_before_action_fails_typed_with_no_action)",
    "supply an unowned/bogus CDP TargetInfo as an OOPIF candidate -> typed rejection at FrameContext::resolve_child, never accepted as a fallback (spider/tests/captcha_browser_oopif_action_real.rs:unowned_target_info_fails_typed_never_a_fallback)",
    "present multiple simultaneous genuine OOPIF targets, only one carrying the real challenge -> action lands only on the correct target's own session, the decoy is never touched (spider/tests/captcha_browser_oopif_action_real.rs:multiple_oopifs_action_targets_only_the_correct_one; spider/tests/captcha_browser_oopif_streaming_shipping_real.rs:website_crawl_shipping_pipeline_isolates_the_correct_oopif_among_several)",
    "force the configured provider to fail -> zero actions dispatched, no silent alternate-provider fallback (spider/tests/captcha_browser_production_binding_real.rs:provider_failure_dispatches_zero_actions)",
    "mutate this capability's own machine-readable ledger evidence (missing OOPIF action production symbol, comment-only symbol, nonexistent VERIFIED test reference, spider_worker false-claimed as production-reachable, macro-shielded PaliGemma routing hop claimed as WIRED) -> the real closure_harness verifier subprocess rejects every one, proven via 5 real, isolated `--exact` mutation tests (spider/tests/closure_harness_behavioral_contract.rs:behavioral_verifier_rejects_missing_oopif_action_production_symbol, behavioral_verifier_rejects_comment_only_captcha_symbol, behavioral_verifier_rejects_evidence_pointing_to_nonexistent_paligemma_test, behavioral_verifier_rejects_production_reachable_claim_for_a_non_calling_artifact, behavioral_verifier_rejects_macro_shielded_paligemma_routing_hop_as_wired)",
    "cite a stale reviewed_commit whose ledger content no longer matches HEAD's substantive fields -> the real closure_harness verifier rejects it (this exact scenario was hit for real while building this commit, when the prior reviewed_commit was tested against the post-partition ledger, and correctly failed before being corrected here)",
]

[stages.CI_ENFORCED]
# The declared structural command below must, and does, structurally
# match this repository's own real, non-gated, applicable `run:` step
# named "Cargo test CAPTCHA CI-portable evidence (chrome
# local_paligemma)" in the spider_core job of .github/workflows/
# rust.yml — verified directly against ci_enforced_commands_are_real_
# required_non_gated_steps_with_exact_feature_match before this table
# was written, not merely asserted. That step also carries the same
# `sudo ip netns exec spider_ci env PATH=<literal> HOME=<literal>
# CARGO_HOME=<literal> RUSTUP_HOME=<literal> RUST_MIN_STACK=67108864`
# prefix as SCORPION_CANONICAL_CI_DETERMINISTIC_NETWORK_LOCKDOWN_
# EXECUTION_001's own deterministic-lib step — recognized only after
# 4c0bfbf5's harness grammar fix (see [stages.ADVERSARIALLY_VERIFIED]
# above), which this reviewed_commit binding already accounts for.
package = "spider"
lib = false
test_targets = ["browser_challenge_detection_real", "captcha_browser_binding_real", "captcha_browser_frame_action_real", "captcha_browser_production_binding_real", "captcha_browser_oopif_action_real", "captcha_browser_oopif_streaming_shipping_real"]
feature_set = "chrome local_paligemma"
positional_filters = []
exact = false
skip = []
ci_workflow_file = ".github/workflows/rust.yml"

[stages.CLOSED]
# required_proof_classes = ["CODE_PROVEN", "OPERATOR_OBSERVED",
# "LIVE_ENVIRONMENT_DEPENDENT"] (deliberately not CI_PROVEN — see
# [stages.ADVERSARIALLY_VERIFIED]'s own comment on why) are all
# present and independently satisfied — verified directly against
# closed_requires_every_declared_proof_class_independently before this
# table was written. closed_commit re-bound to f20fb917 for the exact
# same reason reviewed_commit above was: .github/workflows/rust.yml
# (this capability's own CI_ENFORCED.ci_workflow_file) is an
# unconditional closure_relevant_file, and the prior closed_commit
# (d5765a34) no longer binds it byte-for-byte once SCORPION_CANONICAL_
# MCP_BEHAVIORAL_CI_ENFORCEMENT_001's failure-diagnostics wiring
# landed. Every other closure_relevant_file (spider/tests/
# closure_harness*.rs, spider/Cargo.toml, every known shipping
# artifact's Cargo.toml, every other IMPLEMENTED/VERIFIED/WIRED-cited
# file, spider/src/website.rs, spider/src/page.rs) is untouched by
# that commit — confirmed directly, its diff is .github/workflows/
# rust.yml only, redirecting the same two `build`-job steps' output to
# a log file and invoking the pre-existing .github/scripts/
# report-test-step-failure.sh on failure; the `spider_core` job's own
# CAPTCHA CI step this capability's [stages.CI_ENFORCED] binds is
# byte-for-byte unchanged. No CAPTCHA detection, solving, provider
# routing, frame/OOPIF handling, or CI evidence this capability cites
# changed at all — the change is scoped entirely to spider_mcp's own
# behavioral CI failure diagnostics, a different subsystem.
closed_commit = "f20fb917825e8ed36c237f4598386052cde5aca7"
