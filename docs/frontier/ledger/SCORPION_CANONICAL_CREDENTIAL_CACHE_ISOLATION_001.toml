id = "SCORPION_CANONICAL_CREDENTIAL_CACHE_ISOLATION_001"
sdd = "docs/frontier/CANONICAL_CREDENTIAL_CACHE_ISOLATION_SDD.md"
summary = "Close all live paths where authentication material can influence cache identity, persisted cache content, or remote cache uploads, across both the legacy/vendor-chromey cache and the canonical cache_request.rs executor."
# This field is the harness-computed TRUE current stage, not a historical
# replay of what was originally claimed. That frontier's own closure
# report (commit 725c2b76e3a6bdbad9b153218953bdb3e88a659d) declared
# CLOSED; applying this harness retroactively as the proof case finds the
# evidence only actually supports WIRED — PRODUCTION_REACHABLE.verdict
# below is NOT_MET, so closure_harness.rs withholds every later stage
# regardless of that original claim. See docs/frontier/
# CANONICAL_CLOSURE_AND_PRODUCTION_REALITY_HARNESS_SDD.md section D for
# the full narrative of that discrepancy.
stage = "WIRED"
required_proof_classes = ["CODE_PROVEN", "CI_PROVEN"]

[proof.CODE_PROVEN]
capability_id = "SCORPION_CANONICAL_CREDENTIAL_CACHE_ISOLATION_001"
commit = "725c2b76e3a6bdbad9b153218953bdb3e88a659d"
evidence = [
    "credential-sensitive cache tests and architecture guardrails are locally reproducible",
    "the canonical cache path and credential rejection policy are AST-resolved",
]

[proof.UNPROVEN]
capability_id = "SCORPION_CANONICAL_CREDENTIAL_CACHE_ISOLATION_001"
missing = ["CI_PROVEN", "PRODUCTION_REACHABLE"]
reason = "The capability remains WIRED but is not enabled by a shipping artifact; no CI execution record can promote it."

[stages.DESIGNED]
sdd = "docs/frontier/CANONICAL_CREDENTIAL_CACHE_ISOLATION_SDD.md"

[stages.IMPLEMENTED]
# `chrome` is not in [stages.PRODUCTION_REACHABLE].feature_requirements
# below (that list feeds the reachability *verdict*'s ANY-of-these-
# enabled check, and adding it there would wrongly count any shipping
# artifact that enables plain `chrome` for unrelated reasons as reaching
# this capability). It is genuinely required for
# `spider/src/utils/mod.rs:cache_enabled` to resolve as a real
# definition: that function is gated
# `#[cfg(all(feature = "chrome", feature = "chrome_remote_cache"))]`, and
# Cargo.toml already makes every real `chrome_remote_cache` build imply
# `chrome` (`chrome_remote_cache = ["chrome", ...]`), so this is a
# harmless, accurate restatement of an implied feature — the same
# mechanism `[stages.WIRED].additional_cfg_features` already uses below
# for `sitemap`.
additional_cfg_features = ["chrome"]
evidence = [
    "spider/src/cache_request.rs:fail_closed_on_set_cookie",
    "spider/src/cache_request.rs:materialize_network_response",
    "spider/src/cache_request.rs:fetch_page_html_with_cache_executor",
    "spider/src/utils/mod.rs:cache_enabled",
    "spider/src/utils/mod.rs:contains_disqualifying_secret_header",
    "spider/src/configuration.rs:get_cache_options",
    "vendor/chromey/src/cache/manager.rs:create_cache_key_raw",
    "vendor/chromey/src/cache/manager.rs:contains_disqualifying_secret_header",
]

[stages.VERIFIED]
test_only = true
evidence = [
    "spider/src/cache_request.rs::tests::set_cookie_response_is_never_persisted_or_served_from_cache",
    "spider/src/cache_request.rs::tests::set_cookie_on_a_304_revalidation_response_is_never_persisted",
    "spider/src/cache_request.rs::tests::response_without_set_cookie_still_caches_normally",
    "spider/src/cache_request.rs::tests::request_side_cookie_and_proxy_authorization_headers_still_bypass_cache",
    "spider/tests/architecture_guardrails.rs:chrome_cache_enabled_never_treats_authorized_as_cache_enabled",
    "spider/tests/architecture_guardrails.rs:legacy_write_paths_fail_closed_on_authorized_cache_options",
    "spider/tests/architecture_guardrails.rs:cookie_jar_authentication_participates_in_cache_eligibility",
    "spider/tests/architecture_guardrails.rs:disqualifying_secret_header_check_is_defined_once_per_crate_and_reused",
    "spider/tests/architecture_guardrails.rs:vendor_chromey_cache_key_is_hashed_never_the_raw_credential",
    "spider/tests/architecture_guardrails.rs:canonical_cache_request_fails_closed_on_set_cookie_response",
    "spider/tests/architecture_guardrails.rs:canonical_cache_request_strips_set_cookie_from_304_revalidation_responses",
    "spider/tests/architecture_guardrails.rs:no_shadow_credential_aware_cache_policy_in_cli_or_mcp",
    "vendor/chromey/tests/cache_round_trip.rs::cache_key_never_exposes_a_raw_credential",
    "vendor/chromey/tests/cache_round_trip.rs::put_hybrid_cache_fails_closed_on_set_cookie_response_header",
]
last_verified_command = "cargo test -p spider --lib cache_request --test architecture_guardrails --features \"chrome cache cache_request\"; cargo test --manifest-path vendor/chromey/Cargo.toml --features \"bytes,stream,cache\" --test cache_round_trip"
last_verified_result = "7/7 cache_request unit tests; 228/228 architecture_guardrails; 14/14 vendor/chromey cache_round_trip"

[stages.WIRED]
# `sitemap` is not part of PRODUCTION_REACHABLE.feature_requirements
# below (that list feeds the reachability *verdict*'s ANY-of-these-
# enabled check, and adding it there would change which shipping
# artifacts count as reachable — an unrelated effect). It genuinely is
# required for the second chain below to compile as a real
# implementation rather than the feature-gated no-op stub at
# spider/src/website.rs:12541 (`#[cfg(not(feature = "sitemap"))] pub(crate)
# async fn sitemap_crawl_chain(...) {}`) — closure_harness.rs's
# cfg-aware adjacency check (which resolves the actual feature-active
# overload's body) caught this chain resolving to that stub instead of
# the real `#[cfg(feature = "sitemap")]` overload at line 13395 when
# `sitemap` wasn't included anywhere in this capability's declared
# feature set.
additional_cfg_features = ["sitemap"]
# Both chains below were corrected after operator review found the
# original evidence cited a niche, `decentralized`-feature-only call site
# for the canonical chain — real, but not representative, and not enough
# to distinguish genuine "not dead code" wiring from an isolated island
# nobody reaches. Both chains here are re-derived and grep-verified hop by
# hop, each one rooted at `Website::crawl` — the exact method spider_cli
# and spider_mcp's own source calls (confirmed via grep) — under only the
# feature(s) named in PRODUCTION_REACHABLE.feature_requirements. Neither
# chain requires `decentralized`, `smart`, or any other niche feature.
callers = [
    # The legacy/vendor-chromey path (Website::crawl -> crawl_concurrent
    # -> ... -> set_document_content_if_requested_cached ->
    # vendor/chromey/src/page.rs:spawn_cache_listener -> ... ->
    # create_cache_key_raw) previously appeared here too. It is removed
    # as a WIRED claim, not because the real code changed (no product
    # wiring was touched), but because its one adjacency hop
    # (`set_document_content_if_requested_cached` calling
    # `spawn_cache_listener`) only exists inside a `tokio::join!(...)`
    # macro invocation's arguments in the real source
    # (spider/src/utils/mod.rs) — and this harness no longer credits any
    # macro invocation as call adjacency at all (Codex adversarial
    # review, round 5: "calls inside macro invocations do not establish
    # WIRED adjacency... this harness has no way to tell, from a single
    # file's AST, whether a given `join!(...)` genuinely resolves to
    # `tokio::join!` or to a same-named local shadow — that is real
    # macro resolution, which this harness does not attempt"). The real
    # implementation evidence for that path (vendor/chromey's
    # `create_cache_key_raw`/`contains_disqualifying_secret_header`)
    # remains truthfully claimed under [stages.IMPLEMENTED] above — that
    # stage only claims the fix genuinely exists in the codebase, not
    # that this harness can mechanically prove its reachability from
    # `Website::crawl` through a macro-expanded hop. Proving that
    # reachability again would require either a non-macro call path (if
    # one exists) or a future, narrowly-scoped, explicitly-verified
    # macro-resolution mechanism this harness does not have today — not
    # a re-introduction of the old bare-macro-name allowlist this exact
    # round closed. The canonical `cache_request.rs` path below is
    # unaffected: none of its hops route through a macro invocation at
    # any point, so it remains fully, structurally provable.
    # Canonical cache_request.rs path — reachable only through the
    # sitemap-triggered branch of crawl(), not the main per-page fetch
    # loop above; requires sitemap (see additional_cfg_features above)
    # and cache_request (not enabled by either). `Website::crawl`
    # (spider/src/website.rs:7563) calls BOTH `sitemap_crawl_chain_raw`
    # (inside its `if tor_active` branch, line ~7604) and
    # `sitemap_crawl_chain` (its `else` branch, line ~7607) — a runtime
    # choice, not a compile-time one, so both call sites are real,
    # unconditional presences in `Website::crawl`'s own function body.
    # `closure_harness.rs`'s cfg-aware adjacency check caught that the
    # *previous* hop here, `sitemap_crawl_chain`, has three mutually
    # exclusive cfg-gated overloads; under `chrome` (this workspace's own
    # default and this ledger's own declared feature set) the active one
    # (spider/src/website.rs:13395,
    # `#[cfg(all(feature="sitemap",feature="chrome",not(feature="decentralized")))]`)
    # calls `sitemap_crawl_chrome`, not `sitemap_crawl_raw` — a real,
    # previously-undetected false hop the old (cfg-blind) checker
    # accepted only because a *different*, chrome-excluded overload of
    # the same symbol happened to call `sitemap_crawl_raw`.
    # `sitemap_crawl_chain_raw` (spider/src/website.rs:13413) has no such
    # ambiguity — gated only by `#[cfg(feature = "sitemap")]`, with no
    # chrome-conditional sibling — and its one real body genuinely calls
    # `sitemap_crawl_raw` regardless of whether `chrome` is enabled.
    "spider/src/website.rs:Website::crawl -> spider/src/website.rs:sitemap_crawl_chain_raw -> spider/src/website.rs:sitemap_crawl_raw -> spider/src/website.rs:sitemap_parse_crawl -> spider/src/page.rs:new_page_for_mode -> spider/src/cache_request.rs:fetch_page_html_with_cache_executor",
]

[stages.PRODUCTION_REACHABLE]
reachability_kind = "NOT_REACHABLE"
shipping_artifacts = []
feature_requirements = ["cache_request", "cache", "chrome_remote_cache", "cache_chrome_hybrid"]
# Must be a subset of the root symbol(s) this capability's own
# [stages.WIRED].callers chains actually prove reach its IMPLEMENTED
# evidence (closure_harness.rs cross-checks this) — not an
# independently-declared, disconnected symbol. Both WIRED chains above
# are rooted at Website::crawl, so that is the only valid entry point;
# generic method-name markers like ".crawl(" (which any artifact calling
# .crawl() on anything would trivially satisfy, whether or not it has
# anything to do with this capability) were removed after operator
# review identified them as exactly the kind of unbound "generic symbol"
# evidence this stage must reject.
entry_point_symbols = ["Website::crawl"]
siblings_enumerated = true
siblings = [
    "spider/src/utils/mod.rs:perform_chrome_http_request_cache (structurally proven safe in the source frontier's adversarial pass, same reachability gap applies)",
]
siblings_note = "Every write path this frontier hardened lives behind one of feature_requirements above. No sibling was found that reaches the same behavior through a different, already-enabled feature."
verdict = "NOT_MET"
verdict_evidence = "spider_cli's current default feature set includes shipping research but still includes none of cache_request, cache, chrome_remote_cache, or cache_chrome_hybrid. Its spider dependency also uses default-features = false and does not enable those cache features. spider_mcp likewise forwards none of the required cache features. spider_worker defines cache = [\"spider/cache\"], but its request handlers use Page::new_page_streaming and fetch_page_html_raw rather than Website::crawl/Website::scrape or the canonical cache executor. Compiling a module is not production reachability, so the truthful verdict remains NOT_MET."

# ADVERSARIALLY_VERIFIED, CI_ENFORCED, and CLOSED are deliberately omitted:
# PRODUCTION_REACHABLE.verdict = "NOT_MET" above means closure_harness.rs
# must reject any claim beyond WIRED for this capability, regardless of the
# `stage = "CLOSED"` claim at the top of this file or the real, thorough
# adversarial-review work the source frontier's closure report documents
# against the implementation itself.
